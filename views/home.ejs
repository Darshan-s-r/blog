<%- include("partials/header.ejs") -%>
<h1>home</h1>
<p> <%= StartingContent  %> </p>

<% posts.forEach(function(post) { %>
    <% title = post.title; %>
    <h1> <%= title %> </h1>
    <% body = post.content; %>
    <% body = body.substring(0, 100) + "..."; %>
    <p> <%= body %> <a href="/posts/<%= post._id %>">Read More</a></p>
    <% }); %>
    <a href="/compose" class="link"><button class="add">+</button></a>


<%- include("partials/footer.ejs") -%>